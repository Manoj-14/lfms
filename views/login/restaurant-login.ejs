<%- include('../partials/header.ejs'); %>

    <body>
        <%- include('../partials/login-nav.ejs'); %>
            <section class="login-prof">
                <%- include('../partials/quote-script.ejs'); %>
                    <div class="admin-log">
                        <h1>Restaurant Login</h1>
                        <% if (status) { %>
                            <p class="danger setTimer">
                                <%= status %>
                            </p>
                            <% } %>
                                <form class="admin-login" action="/restAuth" method="post" autocomplete="off">
                                    <div class="material-textfield">
                                        <input placeholder=" " required name="restEmail" class="ngo-user" type="email">
                                        <label>Email</label>
                                    </div>
                                    <div class="material-textfield">
                                        <input placeholder=" " required name="restPassword" class="ngo-pass"
                                            type="password">
                                        <label>Password</label>
                                    </div>
                                    <p style="padding-top: 1rem;margin-bottom: 0;">Don't Have account? <a
                                            style="color: var(--prmy-color);" href="/rest-regs">Sign
                                            Up</a></p>
                                    <div class="material-textfield ">
                                        <input placeholder=" " class="submit" type="submit" value="Sign In">
                                    </div>
                                    <div class="sub-head">
                                        <spam>or</spam>
                                        <h3>Sign IN</h3>
                                        <spam>as</spam>
                                    </div>
                                    <div class="material-textfield" id="other-sign">
                                        <div class="sign-link">
                                            <a href="/admin-login" id="ngo">Admin</a>
                                        </div>
                                        <div class="sign-link">
                                            <a href="/ngo-login" id="restaurant">NGO</a>
                                        </div>
                                    </div>
                                </form>
                                <div>
            </section>
    </body>
    <script>
        const setTimer = document.querySelector(".setTimer");
        console.log(setTimer)
        const myTimeout = setTimeout(rmDisp, 5000);

        function rmDisp() {
            setTimer.style.display = "none";
        }
    </script>

    </html>
